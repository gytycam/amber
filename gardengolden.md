# VastLink Navigator

VastLink Navigator 是一个面向技术社区与内容聚合场景的轻量级外链资源导航系统。项目定位为可自托管的优质站点收录与分发工具，主要服务于需要快速整理、展示和分享大量外部资源链接的技术作者、社区运营者以及个人知识管理爱好者。本项目不提供爬虫、采集或自动化抓取功能，专注于以清晰、可维护的方式组织人工筛选的链接数据，并通过简洁的 Web 界面或 API 输出结构化资源列表。

项目核心解决以下问题：技术文档中散落的大量参考链接难以系统化管理；团队内部需要共享一份可版本控制的资源清单；开源项目 README 或文档站需要动态引用外部站点列表，但不愿重复编写 HTML 表格。VastLink Navigator 通过约定式目录配置和 Markdown 驱动的内容生成，将链接管理成本降至最低。

## 功能概览

- **多源链接聚合管理**：支持将外部 URL 按自定义分类录入 YAML 或 JSON 数据文件，项目启动时自动加载并生成索引。

- **只读 API 服务**：提供 RESTful 风格的只读接口，支持按分类、标签或关键词检索已收录的链接，方便其他应用或脚本调用。

- **静态页面生成模式**：内置简单模板引擎，可将链接数据渲染为独立 HTML 页面，适用于无后端环境的展示场景（如 GitHub Pages）。

- **Markdown 清单导出**：支持一键导出符合本项目 README 格式的链接列表，方便直接复制到其他文档中。

- **链接可用性检查**：提供可选的定时任务，对已收录的 URL 发送 HEAD 请求并记录响应状态，辅助运维人员发现失效链接。

- **分类与标签双维度过滤**：每个链接可关联多个标签和唯一分类，前端或 API 均支持按维度组合筛选。

- **访问统计埋点**：内置轻量级点击计数中间件，可记录链接被访问的次数（基于内存或 Redis 存储），便于分析热点资源。

- **黑白名单机制**：支持配置域名级别的访问控制策略，防止恶意或非预期站点进入展示列表。

## 应用场景

- **开源项目外部依赖索引**：技术项目通常依赖大量第三方文档、工具站或社区讨论帖，VastLink Navigator 可作为官方文档的补充，集中列出这些参考链接，并随项目代码一同版本管理。

- **团队内部知识库导航**：企业内部技术团队可将常用开发工具、内部系统入口、运维面板等链接收录进系统，部署在内网服务器后，团队成员通过统一入口访问，避免收藏夹混乱。

- **技术社区资源推荐页**：垂直技术社区（如编程语言论坛、框架用户组）可利用该项目的静态导出功能，定期生成“本周热门资源”或“精选教程合集”页面，减轻手动维护页面的工作量。

- **个人书签管理替代方案**：对于希望脱离浏览器书签同步功能的高级用户，本项目提供了一种基于纯文本配置的链接管理方式，结合 Git 可实现跨设备同步和历史回溯。

## 快速开始

以下指令适用于 Linux / macOS / Windows WSL 环境。项目基于 Node.js 开发，请确保已安装 Node.js 18.x 及以上版本。

```bash
# 克隆代码仓库
git clone https://github.com/vastlink-navigator/navigator.git
cd navigator

# 安装项目依赖
npm install

# 复制示例配置文件并修改
cp config/default.example.yaml config/default.yaml

# 启动开发服务器（默认监听 3000 端口）
npm run start:dev
```

启动成功后，访问 <code>http://localhost:3000</code> 即可看到示例链接列表。若需生成静态 HTML，可执行：

```bash
npm run build:static
```

静态文件将输出至 `dist/` 目录，可直接部署至任意 HTTP 服务器。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.x 或更高 | 运行时环境，推荐使用 LTS 版本 |
| npm | 9.x 或更高 | 包管理器，随 Node.js 安装 |
| 内存 | 至少 512 MB | 生产环境建议 1 GB 以上 |
| 磁盘空间 | 至少 200 MB | 包含依赖和日志存储 |
| 操作系统 | Linux / macOS / Windows 10+ | 开发测试均在上述平台通过 |
| 数据库（可选） | Redis 7.x | 仅在使用缓存或统计功能时需要 |
| 数据库（可选） | PostgreSQL 14.x | 仅在使用持久化访问记录时需要 |

## 文档导航

| 层面 | 目录 / 文件 | 回答的问题 |
|------|-------------|------------|
| 用户手册 | `docs/guide/getting-started.md` | 如何安装、配置和第一次运行项目？ |
| 运维参考 | `docs/ops/deployment.md` | 生产环境部署选项（Nginx 反向代理、Docker、Systemd）有哪些？ |
| API 参考 | `docs/api/endpoints.md` | 对外提供的所有 HTTP 接口地址、参数和返回格式是什么？ |
| 配置说明 | `docs/config/options.md` | 配置文件中每个字段的含义、默认值和可选值是什么？ |
| 数据规范 | `docs/data/schema.md` | 链接数据文件的字段定义、类型约束和示例数据如何编写？ |

## 资源列表

以下为项目第 335/567 批次收录的外部资源链接。所有链接均按原始格式原样列出，未做任何协议补全或域名标准化处理。

影视娱乐类

- <code>jiqingshipinwangzhi.org.cn</code>
- <code>oumeijingpinzipai.org.cn</code>
- <code>sishilurenqi.org.cn</code>
- <code>mitunjiujiujiu.org.cn</code>
- <code>wuyefulizhibo.org.cn</code>
- <code>lalalazhongwendianshiju.org.cn</code>
- <code>yinghuadongmanguanfangban.org.cn</code>

## 项目结构

```
navigator/
├── config/                           # 配置文件目录
│   ├── default.yaml                  # 主配置文件（端口、数据库、日志级别等）
│   └── custom/                       # 用户自定义配置片段（可覆盖默认值）
├── src/                              # 源代码主目录
│   ├── api/                          # API 路由和控制器
│   │   ├── v1/                       # API 版本 v1 实现
│   │   │   ├── links.js              # 链接查询接口逻辑
│   │   │   └── stats.js              # 统计信息接口逻辑
│   │   └── middleware/               # 身份验证、限流、日志中间件
│   ├── core/                         # 核心业务逻辑
│   │   ├── loader.js                 # 链接数据加载与解析模块
│   │   ├── filter.js                 # 分类/标签过滤引擎
│   │   └── checker.js                # 链接可用性检查任务调度器
│   ├── templates/                    # 静态页面渲染模板（EJS）
│   │   ├── index.ejs                 # 首页列表模板
│   │   └── detail.ejs                # 链接详情页模板
│   ├── utils/                        # 通用工具函数
│   │   ├── logger.js                 # 日志封装（基于 winston）
│   │   └── validator.js              # URL 格式校验与规范化辅助
│   └── app.js                        # 应用入口，初始化 Express 服务
├── data/                             # 用户数据目录（存放链接 YAML/JSON 文件）
│   ├── sources/                      # 按来源分类的链接数据文件
│   └── tags/                         # 标签索引文件（自动生成）
├── test/                             # 单元测试与集成测试用例
│   ├── unit/                         # 单元测试（Mocha + Chai）
│   └── integration/                  # API 端到端测试
├── docs/                             # 完整项目文档（见上方文档导航）
├── scripts/                          # 运维辅助脚本
│   ├── migrate.js                    # 数据格式迁移脚本
│   └── healthcheck.sh                # 服务健康状态检查 Shell 脚本
├── Dockerfile                        # 容器化构建文件（基于 Node.js Alpine）
├── docker-compose.yaml               # 本地开发或测试环境编排（含 Redis/PostgreSQL）
├── package.json                      # npm 项目清单
├── README.md                         # 项目主说明文档（即本文档）
└── LICENSE                           # MIT 许可证文本
```

## 贡献指南

我们欢迎任何形式的贡献，包括但不限于新增功能、修复缺陷、改进文档或提交新的链接资源数据。请遵循以下流程：

1. 在 GitHub 仓库页面点击 `Fork` 创建个人复刻，并将复刻后的仓库克隆至本地。建议在新建的功能分支上工作，分支命名采用 `feature/功能简述` 或 `fix/问题简述` 格式。

2. 完成代码或文档修改后，确保所有现有单元测试通过。若新增了功能，请补充对应的测试用例。运行 `npm run test` 可执行全部测试套件。对于链接数据的增删改，请遵循 `data/schema.md` 中定义的字段规范。

3. 提交前使用 `npm run lint` 检查代码风格，并使用 `npm run format` 自动格式化。提交信息请采用约定式提交规范（Conventional Commits），例如 `feat: 添加按访问量排序功能` 或 `docs: 更新部署文档中的 Nginx 示例`。

4. 将本地分支推送至复刻仓库，然后通过 GitHub 界面发起 Pull Request 到主仓库的 `main` 分支。请在 PR 描述中清晰说明改动内容、动机以及相关的 Issue 编号（如有）。

5. 项目维护者会在一个工作日内审阅 PR，可能会要求修改或补充信息。合并后您的贡献将出现在下一版本发布说明中。

## 常见问题

**问：项目是否必须连接外部数据库才能运行？**

答：不是必须。在默认配置下，项目使用内存存储链接索引和统计计数，适合小型部署或开发测试环境。只有启用访问记录持久化或分布式缓存功能时，才需要配置 Redis 或 PostgreSQL。您可以在 `config/default.yaml` 中关闭相关特性。

**问：如何更新已收录的链接列表，是否需要重启服务？**

答：项目支持热加载数据文件。您可以直接编辑 `data/sources/` 目录下的 YAML 或 JSON 文件，保存后系统会在数秒内自动检测变更并重建内存索引，无需手动重启。若通过 API 添加链接，则需实现自定义持久化逻辑（官方示例中未包含写接口，仅作为只读服务设计）。

**问：静态导出模式下的页面是否可以包含动态搜索功能？**

答：静态导出生成的是纯 HTML + CSS + JavaScript 前端应用，页面本身包含客户端搜索过滤逻辑（基于预置的链接数据 JSON 文件）。因此即使没有后端服务，用户仍可在导出的页面中进行分类筛选和关键词搜索。但链接点击统计功能在静态模式下不可用。

## 许可证

本项目采用 MIT 许可证。您可以在遵守许可证条款的前提下自由使用、修改、分发本软件，包括用于商业目的。完整的许可证文本请参见项目根目录下的 LICENSE 文件。

> 外链数量: 7 | 生成时间: 2026-08-11 03:44:11
