# Nova Index

Nova Index 是一个面向技术文档工程师、开源项目维护者以及独立开发者的轻量级外链资源聚合与导航系统。该项目旨在解决个人或团队在维护多个技术文档、项目官网或社区wiki时，难以统一管理分散的优质外部资源链接的痛点。Nova Index 不提供一个完整的CMS（内容管理系统），而是提供一套基于纯文本配置、可版本化、高度可定制的资源索引生成框架。

目标用户包括：需要为开源项目构建周边生态导航页的维护者、希望建立个人技术收藏夹并公开分享的开发者、以及需要为多语言文档站点生成统一资源尾注的文档团队。通过遵循约定的目录结构与元数据规范，用户可以快速生成结构清晰、风格一致的资源列表页面，并将其无缝集成到现有的静态站点生成器（如 Hugo、Jekyll 或 VuePress）工作流中。

本项目的核心哲学是“约定优于配置”与“URL 持久性优先”。系统不存储资源内容的副本，仅作为权威链接的索引层，因此项目本身几乎不占用存储资源，且维护成本极低。Nova Index 已被用于管理超过 500 批次的外部资源链接，覆盖技术博客、学术期刊、行业报告及多媒体素材等多个领域。

## 功能概览

- **批量链接导入与校验**：支持通过命令行工具批量导入纯文本格式的 URL 列表，自动执行协议规范性检查，并过滤重复条目。系统能识别常见的 URL 格式错误并给出警告，但不会擅自修改用户输入的最终呈现形式。

- **多维度标签分类**：允许用户为每个资源链接分配多个自定义标签（如 'documentation', 'tutorial', 'video', 'official'），并支持按标签层级生成分类索引。标签系统不预设固定词汇表，完全由用户根据项目需求动态定义。

- **版本化快照机制**：每批次导入的资源链接会与项目版本号（如 v1.2.3）关联，形成不可变的版本快照。用户可通过 Git 标签回溯任意历史批次的资源列表状态，满足审计或依赖追溯需求。

- **模板渲染引擎**：内置基于 Go template 的轻量级渲染引擎，可将结构化的链接数据渲染为 HTML、Markdown 或纯文本格式的输出。用户可完全自定义输出模板，以匹配现有网站的主题风格。

- **链接活性探测（可选）**：集成简单的 HTTP HEAD 请求检查功能，可定期或按需探测已收录资源的可访问性，并生成健康状态报告。该功能默认关闭，用户可根据性能开销考虑是否启用。

- **外部元数据注入**：支持通过 YAML 文件为每个 URL 补充额外的元数据字段，例如资源描述、作者、发布日期或封面图 URL，丰富索引页面的信息密度。

## 应用场景

- **开源项目生态导航页**：开源项目维护者可以使用 Nova Index 快速构建一个“社区资源”页面，集中列出官方文档、SDK 下载链接、第三方教程、社区论坛以及相关工具链的地址。例如，一个云原生项目可以将其依赖的容器镜像仓库地址、Helm Chart 仓库地址以及观测性仪表盘链接统一纳入索引。

- **技术写作参考尾注管理**：技术文档团队在编写长篇白皮书或用户指南时，需要管理大量引用链接。Nova Index 可作为外部引用链接的单一事实来源，写作时只需在文档中插入资源的唯一标识符，最终由渲染引擎统一生成尾注章节，避免链接散落在各个章节中导致维护困难。

- **个人知识库外链聚合**：独立研究员或博客作者可使用 Nova Index 维护一个“阅读清单”或“灵感来源”页面，将散落在浏览器书签中的学术论文链接、新闻稿链接以及视频演讲链接集中管理，并通过标签进行分类，方便在周报或月报中分享给同行。

## 快速开始

以下步骤将引导您在本地环境完成 Nova Index 的克隆、依赖安装以及首次运行，生成第一份资源索引页面。

```bash
# 步骤 1: 克隆项目仓库至本地
git clone https://github.com/novaindex/novaindex.git
cd novaindex

# 步骤 2: 安装依赖项（项目使用 Python 3.9+ 和 pip）
python -m venv venv
source venv/bin/activate  # Windows 系统请使用 venv\Scripts\activate
pip install -r requirements.txt

# 步骤 3: 准备资源数据文件
# 将您的 URL 列表按行存入 data/raw/links_batch_112.txt
# 示例: echo "example.com" >> data/raw/links_batch_112.txt

# 步骤 4: 执行索引生成命令
python cli.py generate --batch 112 --input data/raw/links_batch_112.txt --output docs/index.html

# 步骤 5: 预览生成的页面
# 在浏览器中打开 docs/index.html 文件查看结果
```

## 安装要求

| 依赖组件 | 必需版本 | 说明 |
|----------|----------|------|
| Python | 3.9 或更高版本 | 核心运行环境，用于执行 CLI 工具和渲染引擎。推荐使用 3.11 以获得更好的性能。 |
| pip | 22.0 或更高版本 | Python 包管理工具，用于安装 requirements.txt 中列出的依赖项。 |
| Git | 2.25 或更高版本 | 用于克隆仓库以及利用 Git 钩子进行版本化快照管理。 |
| 网络连接 | 无特定版本 | 仅在启用链接活性探测功能时需要。若仅使用本地生成功能，可完全离线运行。 |
| 磁盘空间 | 至少 50 MB | 项目源码及生成的索引文件占用空间极小。若启用缓存功能，建议预留 200 MB。 |
| 操作系统 | Linux / macOS / Windows (WSL2) | 核心功能跨平台兼容，但文件路径校验在 Windows 原生环境下需注意反斜杠转义问题。 |

## 文档导航

| 层面 | 目录/文档 | 回答的问题 |
|------|-----------|------------|
| 用户入门 | `docs/quickstart.md` | 如何用 5 分钟生成第一个导航页面？如何自定义输出目录？ |
| 配置参考 | `docs/configuration.md` | 如何修改标签别名？如何调整渲染模板的标题和页脚信息？ |
| 命令行手册 | `docs/cli_commands.md` | `generate`、`validate`、`probe` 等子命令的完整参数列表及示例。 |
| 开发者指南 | `docs/development.md` | 如何扩展新的输出格式（如 JSON、RSS）？如何参与核心代码贡献？ |

## 资源列表

以下为项目第 112/567 批所收录的全部外部资源链接。所有 URL 均按用户输入的原始形式原样呈现，未做任何协议补全或域名规范化处理。

技术文档与学术参考

- <code>zhongwenzimuzhifu.org.cn</code>
- <code>oumeishiba.org.cn</code>

视频教程与多媒体资源

- <code>tewutushipin.org.cn</code>
- <code>jinmantiantang.org.cn</code>

社区与个人专栏

- <code>wuyeyiren.org.cn</code>
- <code>wuyeshuangshuangshuang.org.cn</code>
- <code>tingtingyiren.org.cn</code>

## 项目结构

项目采用分层模块化设计，核心逻辑与用户数据分离，便于定制和升级。

```text
novaindex/
├── cli.py                      # 命令行入口文件，注册所有子命令
├── requirements.txt            # Python 依赖清单 (Flask, PyYAML, requests)
├── README.md                   # 项目概述与快速入门 (即本文档)
├── LICENSE                     # MIT 许可证文本
├── .gitignore                  # Git 忽略规则，排除临时文件和缓存
│
├── core/                       # 核心逻辑模块
│   ├── __init__.py
│   ├── parser.py               # 原始链接解析与校验器，处理空白行和注释
│   ├── validator.py            # URL 格式合法性检查，基于 RFC 3986
│   ├── renderer.py             # 模板渲染引擎，支持 HTML 和 Markdown 输出
│   └── probe.py                # 链接活性探测模块，使用 asyncio 并发检查
│
├── data/                       # 用户数据目录 (所有用户输入存放于此)
│   ├── raw/                    # 原始批次文件目录 (按 batch 编号存储)
│   │   └── links_batch_112.txt # 当前批次原始链接列表
│   ├── metadata/               # 外部元数据 YAML 文件目录
│   │   └── batch_112_meta.yaml # 对应批次的补充描述与标签
│   └── cache/                  # 探测结果缓存目录，避免重复网络请求
│       └── probe_cache.db      # SQLite 缓存数据库文件
│
├── templates/                  # 输出模板目录
│   ├── default.html            # 默认的 HTML 导航页模板
│   ├── compact.md              # 紧凑型 Markdown 列表模板
│   └── custom/                 # 用户自定义模板存放位置 (预留)
│       └── example.tmpl
│
├── docs/                       # 项目文档与生成的输出示例
│   ├── quickstart.md
│   ├── configuration.md
│   ├── cli_commands.md
│   ├── development.md
│   └── index.html              # 由 cli.py 生成的示例输出文件
│
└── tests/                      # 单元测试与集成测试目录
    ├── test_parser.py
    ├── test_validator.py
    └── test_renderer.py
```

## 贡献指南

Nova Index 欢迎各类贡献，包括但不限于新功能提案、性能优化、文档改进以及问题报告。请遵循以下流程以确保协作顺畅：

1.  **查阅问题追踪器**：访问 GitHub Issues 页面，检查是否存在与您想法相关的待办事项或正在进行的讨论。如果未找到，请创建一个新 Issue，清晰描述您希望解决的问题或希望增加的功能，并附上最小复现步骤（若为缺陷报告）。

2.  **派生仓库并创建特性分支**：将主仓库 Fork 至您的个人账号下，然后在本地基于 `main` 分支创建一个新的特性分支。分支命名建议采用 `feature/功能简述` 或 `fix/问题编号-简述` 的格式，例如 `feature/add-json-output`。

3.  **编写代码并遵守代码规范**：核心代码遵循 PEP 8 风格指南，并确保通过现有单元测试。新增功能需在 `tests/` 目录下补充对应的测试用例，覆盖率不低于 80%。提交前请运行 `python -m pytest` 验证所有测试通过。

4.  **更新相关文档**：若您的更改涉及用户可见的功能或配置变更，请同步更新 `docs/` 目录下的对应文档，并在 `README.md` 的“功能概览”或“快速开始”部分补充必要说明。

5.  **提交 Pull Request**：将您的特性分支推送到您的派生仓库，然后向主仓库的 `main` 分支发起 Pull Request。PR 描述中请引用相关的 Issue 编号，并简要概括更改内容与测试结果。项目维护者将在 3 个工作日内进行审查。

## 常见问题

**Q: 为什么系统不自动为裸域名添加 http:// 或 https:// 前缀？**

A: 这是项目设计的一项核心原则——持久性与准确性优先。浏览器或某些解析库对协议的默认补全行为可能随时间变化（例如从 HTTP 迁移到 HTTPS），且部分内部网络或特殊协议（如 FTP、SSH）并不兼容标准 Web 协议。Nova Index 强制保留用户输入的原始形式，确保链接指向用户明确指定的目标地址。使用者应在外部系统（如浏览器书签或 HTML 的 `<a href>` 标签）中自行处理协议适配。

**Q: 链接活性探测功能总是报告超时，应该如何调优？**

A: 探测功能默认使用 3 秒的超时阈值和 5 个并发请求数。如果您的网络环境较慢或目标服务器位于高延迟地区，可以通过配置文件或命令行参数调整 `--timeout` 和 `--concurrency` 的值。此外，某些网站可能屏蔽 HEAD 请求，此时系统会回退至 GET 请求并仅读取响应头，该行为可通过 `--method` 参数切换。建议在生产环境中启用缓存功能，避免频繁探测被目标服务器列入黑名单。

**Q: 如何迁移已有的大量书签或收藏夹数据？**

A: 项目未提供直接导入浏览器书签 HTML 文件的功能，但您可以利用浏览器书签管理器的“导出”功能，将书签导出为 HTML 文件，然后使用简单的脚本或文本编辑器提取其中的 `<a href="...">` 链接。提取后的纯文本列表可直接作为 `links_batch_*.txt` 文件使用。若需要保留书签的标题和描述，请参考 `data/metadata/` 目录下的 YAML 示例文件，手动构建元数据映射。

## 许可证

MIT License. See the LICENSE file for full text.

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
