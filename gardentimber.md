# NovaScore

NovaScore 是一个面向竞技体育数据分析与实时比分聚合的开源技术资源导航站。项目聚焦于为体育数据分析师、赛事运营人员以及体育科技开发者提供高可用、低延迟的比分数据源索引与状态监控工具。NovaScore 本身不存储或生产任何赛事数据，而是通过规范化的外链聚合与可用性探测机制，帮助用户快速定位稳定、权威的第三方比分与赛程数据接口，解决赛事数据获取过程中源地址分散、可用性不稳定以及文档缺失等实际问题。

项目目标用户包括体育数据平台的后端工程师、体育博彩风控系统的数据运维人员、以及高校体育科学实验室的研究助理。NovaScore 通过定期对收录的比分数据源进行 HTTP 可达性校验与响应时长记录，为每个数据源生成健康度评分，并以仪表板形式呈现，从而辅助用户在多个备选数据源之间实现智能切换或故障转移。项目完全基于静态站点生成技术，无服务端依赖，可部署于任何支持 HTTP 静态托管的 CDN 或对象存储服务。

## 功能概览

- **数据源健康度探测**：系统内置定时任务，每五分钟对全部收录的比分与赛程数据源发起 HEAD 及 GET 请求，记录状态码、响应时间及内容哈希变化，生成历史可用性趋势图表。

- **结构化元数据索引**：每个数据源均附带地区、语言、数据更新频率、历史数据回溯能力、是否提供 JSONP 支持等元标签，支持多维度筛选与排序。

- **快速故障转移建议**：当主用数据源连续三次探测失败时，系统自动从备用列表中按响应时间升序推荐候选地址，并提供一键复制替换配置的代码片段。

- **数据源变更通知**：通过 RSS 订阅或 Webhook 回调，订阅者可在数据源 URL 变更、TLS 证书即将过期或响应结构发生破坏性调整时收到预警邮件。

- **历史存档与对比**：每日零点对全部数据源返回的样本数据进行快照存档，支持按时间轴对比同一数据源在不同日期的响应体结构差异，辅助定位字段废弃或新增情况。

- **开放数据源提交审核**：社区成员可通过 Pull Request 或 Issue 模板提交新的比分数据源，经维护组审核后合并入公共索引库，审核过程包含可用性三阶段验证。

- **轻量化部署与离线使用**：生成完整的静态 HTML 仪表板，包含所有数据源信息及最近七天的健康度历史，无需数据库或运行时环境，可直接通过文件协议在离线环境中打开浏览。

## 应用场景

- **体育数据中台的备用源配置**：数据平台架构师在搭建多活数据管道时，需要为每类赛事（如足球、篮球、网球）配置至少三个可用的比分源。NovaScore 提供的健康度评分和响应时长百分位数统计，可直接用于权重分配算法的输入参数，降低因单一源故障导致的管道中断风险。

- **赛事直播应用的低延迟切换**：面向终端用户的比分直播移动应用，在比赛高峰期对数据延迟极为敏感。运维人员可使用 NovaScore 的实时探测面板监控各数据源的出口延迟，当当前源延迟超过阈值时，人工或自动化脚本依据 NovaScore 输出的备用列表进行秒级切换。

- **体育数据科学研究的前期调研**：高校研究团队在开展足球比赛结果预测或球员表现分析项目时，需要评估不同数据源对历史比赛覆盖的完整度。NovaScore 的存档对比功能可帮助研究人员在数小时内完成对多个数据源近一周数据一致性的初步验证，而非花费数天进行人工抽检。

## 快速开始

以下步骤帮助您在本地环境中快速启动 NovaScore 的仪表板服务。

```bash
# 克隆项目仓库至本地
git clone https://github.com/novascore/novascore-index.git

# 进入项目根目录
cd novascore-index

# 安装依赖（项目使用 Node.js 与 pnpm 进行构建）
pnpm install

# 执行数据源探测与静态站点生成
pnpm run build

# 启动本地预览服务器，默认监听端口 8080
pnpm run preview
```

执行上述命令后，打开浏览器访问 `http://localhost:8080` 即可查看当前收录的所有数据源列表及其健康度仪表板。若需自定义探测间隔或筛选规则，可编辑项目根目录下的 `config.yaml` 文件。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 18.17.0 或更高 | 运行时环境，用于执行构建脚本与探测任务 |
| pnpm | 8.6.0 或更高 | 包管理器，用于安装项目依赖 |
| Git | 2.30.0 或更高 | 版本控制工具，用于克隆仓库及提交变更 |
| 网络连接 | 出站 80/443 端口开放 | 用于对收录的数据源执行 HTTP 探测请求 |
| 操作系统 | Linux / macOS / Windows (WSL2) | 开发与部署环境，Windows 原生 PowerShell 可能存在脚本兼容性问题，推荐使用 WSL2 |
| 浏览器 | 支持 ES2020 的现代浏览器 | 用于查看生成的仪表板 HTML 文件 |
| 磁盘空间 | 至少 200 MB | 用于存储项目源码、依赖及历史存档快照 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户手册 | `/docs/user-guide/` | 如何配置探测频率、如何筛选数据源标签、如何解读健康度评分曲线 |
| 运维手册 | `/docs/ops-guide/` | 如何部署到生产环境、如何配置 Webhook 通知、如何调整存档保留策略 |
| 开发手册 | `/docs/dev-guide/` | 如何新增自定义探测插件、如何修改仪表板主题、如何编写单元测试 |
| 数据源提交规范 | `/docs/submission-guide/` | 提交新数据源需要满足哪些字段要求、审核流程耗时多久、如何更新已有数据源的元信息 |
| FAQ | `/docs/faq/` | 常见构建错误排查、数据源响应格式变更如何处理、如何回滚到上一版本索引 |

## 资源列表

### 赛程与比分数据源（主站）

<code>ouguanbifensaicheng.org.cn</code>

<code>ouguanbifen.org.cn</code>

### 足球比分数据源

<code>nuochaozuqiubifenwang.org.cn</code>

<code>nuochaozuqiubifen.org.cn</code>

### 赛程聚合数据源

<code>nuochaosaicheng.org.cn</code>

### 实时比分数据源

<code>nuochaojishibifen.org.cn</code>

### 积分榜数据源

<code>nuochaojifenbang.org.cn</code>

## 项目结构

```
novascore-index/
├── config/                         # 项目全局配置目录
│   ├── sources.yaml                # 收录的所有数据源 URL 及元信息定义
│   ├── probes.yaml                 # 探测策略配置（超时、重试、间隔）
│   └── notifications.yaml          # Webhook 与邮件通知渠道配置
├── src/                            # 核心源代码目录
│   ├── core/                       # 核心探测引擎
│   │   ├── checker.js              # 单数据源健康检查逻辑实现
│   │   ├── scheduler.js            # 定时任务调度与并发控制
│   │   └── archive.js              # 历史快照存档与差异比对模块
│   ├── ui/                         # 仪表板前端资源
│   │   ├── templates/              # HTML 模板文件
│   │   ├── static/                 # CSS 样式与 JavaScript 脚本
│   │   └── components/             # 可复用的仪表板组件（图表、表格）
│   └── utils/                      # 通用工具函数
│       ├── network.js              # HTTP 请求封装与 TLS 证书提取
│       ├── parser.js               # 响应内容解析与字段校验
│       └── logger.js               # 结构化日志输出与日志轮转
├── tests/                          # 单元测试与集成测试目录
│   ├── unit/                       # 各模块独立单元测试
│   └── integration/                # 端到端探测流程集成测试
├── docs/                           # 完整项目文档（参见文档导航）
├── archive/                        # 运行时生成的历史存档数据（默认 .gitignore）
├── dist/                           # 构建输出的静态站点文件（默认 .gitignore）
├── .github/                        # GitHub 社区模板与 CI 配置
│   ├── workflows/                  # GitHub Actions 持续集成流水线
│   └── ISSUE_TEMPLATE/             # 数据源提交与问题反馈模板
├── package.json                    # Node.js 项目声明与依赖列表
├── pnpm-lock.yaml                  # 依赖锁定文件
├── .eslintrc.js                    # JavaScript 代码规范配置
├── .prettierrc                     # 代码格式化配置
└── README.md                       # 项目根目录说明文档（本文件）
```

## 贡献指南

1.  Fork 本仓库至您的个人 GitHub 账户，并克隆到本地开发环境。确保您的开发环境已满足上述安装要求中的所有依赖项版本。

2.  在 `config/sources.yaml` 文件中按照既定格式添加新的数据源条目，或对现有条目的元信息进行修正。所有新增数据源必须包含完整的 `url`、`region`、`sport_type` 和 `update_interval` 字段。

3.  提交变更前，请务必在本地执行 `pnpm run test` 以确认所有单元测试和集成测试通过。若新增数据源涉及特殊解析逻辑，需在 `tests/unit/` 下补充对应的测试用例。

4.  编写清晰的提交信息，遵循 Conventional Commits 规范（如 `feat: add new basketball source` 或 `fix: correct probe timeout for soccer source`）。提交后推送到您的 Fork 仓库。

5.  通过 GitHub 界面发起 Pull Request 到本仓库的 `main` 分支。PR 描述中请注明所添加或修改的数据源列表，并附上本地探测成功的截图或日志片段。维护组将在两个工作日内完成审核与合并。

## 常见问题

**问：探测任务返回 HTTP 200 但数据内容为空或格式错误，系统如何处理？**

答：NovaScore 的探测引擎在获取响应后会执行两层校验。第一层检查 HTTP 状态码是否为 2xx，第二层会依据 `config/sources.yaml` 中为该数据源定义的 `expected_fields` 列表验证响应体是否包含必要字段（如 `status`、`data`、`matches`）。若第二层校验失败，该探测结果会被标记为 `CONTENT_INVALID`，不计入可用性成功统计，但会记录详细的错误原因至日志文件中供后续排查。

**问：如何调整数据源的探测超时时间？默认值对于海外源可能过短。**

答：您无需修改核心代码。所有探测超时配置均集中在 `config/probes.yaml` 文件中，其中 `timeout_ms` 字段为全局默认超时值。若需为特定数据源单独设置超时，可在 `config/sources.yaml` 中为对应条目添加 `probe_timeout` 覆盖字段，单位为毫秒。修改后执行 `pnpm run build` 即可生效。

**问：历史存档数据会无限制增长吗？如何设置自动清理策略？**

答：项目默认不自动清理存档数据。您可以在 `config/archive.yaml` 文件中配置 `retention_days` 参数，指定保留最近多少天的快照文件。系统在执行每日零点存档任务前会自动检查并删除早于保留天数的旧存档。若该配置文件不存在，系统将保留全部存档直至手动清理。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
