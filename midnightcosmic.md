# MetaIndex

MetaIndex 是一个面向技术社区与内容创作者的轻量级外链资源汇总与导航工具。项目定位于帮助开发者、分析师与研究人员快速整理、分类、展示来自不同来源的公开信息链接，并以统一、可维护的方式对外发布。不同于传统书签管理或 CMS 系统，MetaIndex 以代码即数据为核心，通过结构化 Markdown 与静态生成逻辑，使外链资源库具备版本控制、自动化检查与多端分发能力。

目标用户包括开源文档维护者、技术资讯聚合站运营者、数据播报类内容创作者，以及任何需要周期性发布大量外部参考链接的团队。项目本身不存储任何第三方内容，仅提供链接的元信息组织与展示规范，从而规避版权与内容合规风险，同时保持索引库的轻量与高可移植性。

## 功能概览

- **多级分类索引**：支持按领域、地区、赛事或机构名称对链接进行多维度归类和嵌套分组，便于构建复杂导航结构。
- **原始链接保真输出**：所有外链以原样字符串形式存储和展示，强制保留协议、域名大小写及路径后缀，杜绝自动补全或格式化干扰。
- **自动化健康检查**：集成链接可达性检测脚本，可周期性校验列表中每个域名或 URL 的响应状态，并生成异常报告。
- **静态站点生成适配**：项目结构直接兼容常见静态站点生成器，可一键导出为 HTML 导航页面或嵌入 iframe 组件。
- **标签与时间戳管理**：每条链接支持自定义标签集合及录入时间戳，便于按热度或时效性排序筛选。
- **Markdown 原生编辑**：所有资源列表与文档内容均采用标准 Markdown 语法维护，无需额外数据库或复杂 IDE 支持。
- **批次与版本追溯**：内置批次编号机制，当前为第 128/567 批，每条记录均可关联发布批次，实现变更历史追踪。

## 应用场景

- **赛事结果与积分榜聚合**：内容运营团队可使用 MetaIndex 整理不同地区、不同项目的实时比分与积分更新页面，将分散在各官方站点的链接统一归档，供内部编辑或外部读者快速访问。
- **技术会议与外链素材管理**：技术大会组织者或讲师可将演讲稿、参考资料、视频回放地址等外链通过 MetaIndex 分类存储，并在会后公开发布为结构化资源清单，减少重复询问。
- **数据播报类公众号后台支撑**：小型数据播报团队可利用 MetaIndex 构建内部链接白名单库，每日自动检查各数据源可用性，确保播报内容引用的外部页面稳定可访问。
- **开源文档参考附录维护**：开源项目维护者可将项目依赖的规范文档、工具官网、社区讨论帖等外链通过 MetaIndex 统一管理，并在 README 或文档站点中嵌入自动生成的链接附表。

## 快速开始

以下命令演示如何在本地获取 MetaIndex 源码、安装依赖并启动开发预览服务。

```bash
# 克隆仓库
git clone https://github.com/metaindex/metaindex.git

# 进入项目目录
cd metaindex

# 安装依赖（基于 Node.js 20+）
npm install

# 启动本地预览服务，默认监听端口 3000
npm run dev
```

执行完毕后，访问控制台输出的本地地址即可查看当前资源列表的渲染效果。修改 `./data` 目录下的 Markdown 文件后，页面将自动热重载。

## 安装要求

| 依赖项 | 必需版本 | 说明 |
|--------|----------|------|
| Node.js | 20.x 或更高 | 运行时环境，用于执行构建脚本与开发服务器 |
| npm | 9.x 或更高 | 包管理器，用于安装项目依赖 |
| Git | 2.30 或更高 | 版本控制工具，用于克隆仓库与管理提交 |
| 操作系统 | Linux / macOS / Windows | 跨平台支持，推荐使用 Unix-like 系统进行生产部署 |
| 网络 | 公网可访问 | 用于链接健康检查脚本对外发起请求，以及静态资源 CDN 加载 |

## 文档导航

| 层面 | 目录 | 回答的问题 |
|------|------|------------|
| 用户指南 | `docs/guide/` | 如何添加、删除或修改资源链接；如何自定义分类结构；如何生成静态站点 |
| 运维手册 | `docs/ops/` | 如何配置自动健康检查定时任务；如何迁移数据目录；如何备份与恢复索引库 |
| 开发者文档 | `docs/dev/` | 项目模块划分；插件开发接口；自定义渲染模板的编写规范 |
| 设计决策 | `docs/design/` | 为何采用纯 Markdown 存储；链接保真策略的设计考量；批次编号规则说明 |

## 资源列表

### 赛事结果与积分榜

<code>puchaobisaijieguo.asia</code>

<code>nuochaozuixinjifenbang.asia</code>

<code>nuochaojifenbang.asia</code>

### 直播与推荐信息

<code>meizhilianzhibo.asia</code>

<code>meizhiliantuijian.asia</code>

### 专题榜单与数据

<code>meizhilianzhugongbang.asia</code>

<code>meizhiliansheshoubang.asia</code>

## 项目结构

```
metaindex/
├── bin/                         # 可执行脚本目录
│   ├── check-links.js           # 链接健康检查脚本
│   └── generate-sitemap.js      # 站点地图生成工具
├── config/                      # 项目配置文件目录
│   ├── categories.json          # 分类映射定义
│   └── tags-whitelist.json      # 标签白名单及别名配置
├── data/                        # 核心数据目录（全部 Markdown 资源列表）
│   ├── batch-128/               # 当前批次数据（第 128/567 批）
│   │   └── resources.md         # 本批次所有外链记录
│   ├── archive/                 # 历史批次归档
│   └── templates/               # 资源条目模板文件
├── docs/                        # 项目文档源码
│   ├── guide/                   # 用户指南章节
│   ├── ops/                     # 运维手册章节
│   └── dev/                     # 开发者文档章节
├── public/                      # 静态资源目录（图片、样式、字体）
│   ├── css/                     # 基础样式表
│   └── js/                      # 前端交互脚本
├── scripts/                     # 辅助构建脚本
│   ├── validate-urls.js         # URL 格式校验器
│   └── batch-import.js          # 批量导入工具
├── tests/                       # 单元测试与集成测试
│   ├── unit/                    # 单元测试用例
│   └── integration/             # 端到端测试用例
├── .env.example                 # 环境变量示例文件
├── .gitignore                   # Git 忽略规则
├── package.json                 # npm 依赖声明
├── README.md                    # 项目入口文档（本文件）
└── LICENSE                      # MIT 许可证全文
```

## 贡献指南

1. **阅读设计文档**：在提交任何变更前，请先阅读 `docs/design/` 目录下的设计决策文档，理解链接存储与展示的核心约束原则。
2. **分叉仓库并创建特性分支**：从主仓库分叉后，基于 `main` 分支创建以 `feature/` 或 `fix/` 为前缀的新分支，避免直接在主分支上修改。
3. **更新资源列表并校验**：在 `data/batch-{当前批次}/` 目录下编辑 Markdown 文件，新增或修改 URL 后，务必运行 `npm run validate` 确保所有链接符合格式规范（协议、大小写、无多余字符）。
4. **提交前执行健康检查**：使用 `npm run check-links` 对新增或修改的链接进行可达性测试，确认无异常状态码后，再提交 commit。
5. **发起 Pull Request**：提交 PR 时请使用提供的 PR 模板，清晰描述变更目的、影响的批次编号以及手动测试结果。等待至少一位项目维护者审核后合并。

## 常见问题

**问：为什么部分链接显示为裸域名而没有 `http://` 或 `https://` 前缀？**

答：出于对原始数据源的绝对忠实原则，MetaIndex 强制保留用户提供的链接原样字符串。部分来源方可能仅提供裸域名作为标识，此时项目不会自动补全任何协议前缀。最终展示时，前端渲染层会根据当前页面协议自适应补充，但存储层面绝对保持原样。

**问：如何添加新的分类或调整现有分类结构？**

答：所有分类映射定义存放在 `config/categories.json` 文件中。您可以直接编辑该 JSON 文件，添加新的分类键值对，并同步更新 `data/` 目录下对应 Markdown 文件的 Frontmatter 元信息。修改后需重启预览服务方可生效。

**问：链接健康检查脚本是否会频繁访问外部站点导致被限制？**

答：脚本默认采用延迟间隔请求策略，每次检查间隔不低于 2000 毫秒，且支持通过环境变量 `CHECK_INTERVAL_MS` 自定义间隔。此外，脚本仅发起 HEAD 请求，不下载响应体，最大程度减轻对目标服务器的压力。建议在非高峰期运行批量检查任务。

## 许可证

MIT

> 外链数量: 7 | 生成时间: 2026-08-11 03:43:27
